<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="#">Carnet</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <!-- Conteneur Flex pour les éléments de navigation -->
      <div class="d-flex w-100 justify-content-between">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active text-white" href="{{ path('app_home') }}">Home</a>
          </li>
            {% if app.user %}
             <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Carnet</a>
          <div class="dropdown-menu">
           
            <a class="dropdown-item" href="{{ path('app_contact') }}" href="#">Mes Contacts</a>
            <a class="dropdown-item" href="{{ path('app_contact_add') }}" href="#">Créer un contact</a>
        </li>
          {% endif %}
        </ul>
        <ul class="navbar-nav">
           <li class="nav-item">
          {% if app.user %}
            {% else %}
           <a class="nav-link active text-white" href="{{ path('app_register') }}">Créer un compte</a>
            {% endif %}
          </li>
          <li class="nav-item">
          {% if app.user %}
            <a class="nav-link active text-white" href="{{ path('app_logout') }}">Logout</a>
            {% else %}
             <a class="nav-link active text-white" href="{{ path('app_login') }}">Login</a>
            {% endif %}
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
